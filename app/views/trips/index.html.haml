%ul.polaroids
  - for trip in @trips
    %li
      %a{ :href => trip_path( trip ), :title => trip.location }
        = image_tag trip.photo.url( :thumb )
.clear

.toolbar
  = link_to( t('Upload a Picture'), new_trip_path )
